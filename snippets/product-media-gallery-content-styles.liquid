{%- doc -%}

  @example
  {% render 'product-media-gallery-content-styles' %}
{%- enddoc -%}

{% stylesheet %}
  media-gallery:where(.media-gallery--grid) .media-gallery__grid {
    display: none;
  }

  media-gallery.media-gallery--grid .media-gallery__grid .product-media-container {
    height: 100%;
  }

  @media screen and (min-width: 750px) {
    .media-gallery--two-column .media-gallery__grid {
      grid-template-columns: repeat(2, 1fr);
    }

    media-gallery:is(.media-gallery--grid) slideshow-component {
      display: none;
    }

    media-gallery:where(.media-gallery--grid) .media-gallery__grid {
      display: grid;
    }
  }

  zoom-dialog dialog {
    width: 100vw;
    height: 100vh;
    border: none;
    margin: 0;
    padding: 0;
    max-width: 100%;
    max-height: 100%;
    background: #fff;
    opacity: 0;
    transition: opacity var(--animation-speed) var(--animation-easing);
    scrollbar-width: none;

    &[open] {
      opacity: 1;
    }

    @media (prefers-reduced-motion: no-preference) {
      scroll-behavior: smooth;
    }

    &::backdrop {
      background: transparent;
    }
  }

  .product-media-container__zoom-button {
    position: absolute;
    width: 100%;
    height: 100%;
    z-index: var(--layer-flat);
    cursor: zoom-in;
    background-color: transparent;

    &:hover {
      background-color: transparent;
    }
  }

  slideshow-slide.product-media-container--tallest {
    content-visibility: visible;
  }

  .product-media__drag-zoom-wrapper {
    aspect-ratio: inherit;
    min-height: inherit;
    min-width: inherit;
    display: inherit;
    flex: inherit;
  }

  .media-gallery__mobile-controls {
    grid-area: auto;
  }

  .product-media-container--zoomable.product-media-container--image {
    cursor: zoom-in;
  }

  /* =========================================================
     Figma match: cap main gallery media height (desktop)
     Scoped to slideshow slides so the fullscreen zoom view
     is left untouched.
     ========================================================= */
  @media screen and (min-width: 750px) {
    media-gallery slideshow-slide.product-media-container {
      max-height: 540px;
    }
    media-gallery slideshow-slide.product-media-container .product-media {
      aspect-ratio: auto;
      height: 540px;
    }
    media-gallery slideshow-slide.product-media-container
      :is(.product-media__image, img, .deferred-media__poster-image) {
      height: 100%;
      object-fit: contain;
    }
  }

  /* =========================================================
     Figma match: round, green-outlined carousel arrows.
     Forced over the theme defaults (blend mode + accent ring).
     ========================================================= */
  media-gallery slideshow-arrows .slideshow-control,
  .media-gallery--carousel slideshow-arrows .slideshow-control {
    width: 48px !important;
    height: 48px !important;
    padding: 0 !important;
    border: 2px solid #0f3d2e !important;
    border-radius: 50% !important;
    background: #ffffff !important;
    color: #0f3d2e !important;
    mix-blend-mode: normal !important;
    box-shadow: none !important;
    outline: none !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    /* neutralise the theme's accent variables that tint the ring */
    --slideshow-control-color: #0f3d2e !important;
    --color-primary: #0f3d2e !important;
  }

  media-gallery slideshow-arrows .slideshow-control:hover {
    background: #f4f4f4 !important;
  }

  /* Some Horizon versions wrap the icon in an inner element that
     carries the blend mode / colour — neutralise it too. */
  media-gallery slideshow-arrows .slideshow-control > * {
    mix-blend-mode: normal !important;
    background: transparent !important;
    color: #0f3d2e !important;
  }

  media-gallery slideshow-arrows .slideshow-control svg,
  media-gallery slideshow-arrows .slideshow-control svg * {
    mix-blend-mode: normal !important;
    color: #0f3d2e !important;
    fill: #0f3d2e !important;
    stroke: #0f3d2e !important;
    width: 20px !important;
    height: 20px !important;
  }
{% endstylesheet %}
